Избрал съм този терминал като първия който желая да направя свой. Това за мен е първата стъпка към ставейки [[Becoming a senior dev|сташи програмист]].

# .Config
```lua
local wezterm = require 'wezterm'

local config = {}

  

config.color_scheme = 'Black Metal (Burzum)'

config.window_background_opacity = 0.77

  

return config
```